{
  "_comment": "Copy the mcpServers block into your Claude Desktop config. On macOS this lives at ~/Library/Application Support/Claude/claude_desktop_config.json. Replace /ABS/PATH/TO/vcf2report with the absolute path to this repo. After editing, fully quit and reopen Claude Desktop.",
  "mcpServers": {
    "vcf2report": {
      "command": "python3",
      "args": ["-m", "vcf2report.mcp_server"],
      "env": {
        "PYTHONPATH": "/ABS/PATH/TO/vcf2report/src",
        "VCF2REPORT_DATA": "/ABS/PATH/TO/vcf2report/data",
        "VCF2REPORT_TEMPLATES": "/ABS/PATH/TO/vcf2report/templates",
        "OFFLINE": "0"
      }
    }
  }
}
